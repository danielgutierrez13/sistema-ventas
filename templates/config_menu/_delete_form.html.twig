<form method="post" action="{{ path('config_menu_delete', {'id': config_menu.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ config_menu.id) }}">
    <button class="btn">Delete</button>
</form>
