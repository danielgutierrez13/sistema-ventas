<form method="post" action="{{ path('unidad_medida_delete', {'id': unidad_medida.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ unidad_medida.id) }}">
    <button class="btn">Delete</button>
</form>
