{% extends 'base.html.twig' %}

{% block title %}UsuarioRol{% endblock %}

{% block body %}
    <h1>UsuarioRol</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ usuario_rol.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ usuario_rol.nombre }}</td>
            </tr>
            <tr>
                <th>Rol</th>
                <td>{{ usuario_rol.rol }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{{ usuario_rol.activo ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('usuario_rol_index') }}">back to list</a>

    <a href="{{ path('usuario_rol_edit', {'id': usuario_rol.id}) }}">edit</a>

    {{ include('usuario_rol/_delete_form.html.twig') }}
{% endblock %}
